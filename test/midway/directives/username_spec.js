/*
describe("Testing AngularJS Directives", function() {

  var test;

  before(function(done) {
    test = new YOMngTest();
    test.register(App, 'App', done);
  });

  beforeEach(function() {
    test.reset();
  });

  it('should be empty', function() {
    test.view().innerHTML.should.eq('');
  });

  it('should load the directive properly', function() {
    var elm = test.directive('<div data-username></div>');
    elm.text().should.eq('username');
  });

  it('this should not be a registered directive', function() {
    var elm = test.directive('<div data-username2></div>');
    elm.text().should.eq('');
  });

});
*/
